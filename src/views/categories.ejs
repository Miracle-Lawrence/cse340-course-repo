<%- include('partials/header') %>

<main>
  <h1><%= title %></h1>

  <ul>
  <% categories.forEach((category, category_id) => { %>
    <li>
      <a href="/category/<%= category_id + 1 %>">
        <%= category.name %>
      </a>
    </li>
  <% }) %>
</ul>
</main>

<%- include('partials/footer') %>